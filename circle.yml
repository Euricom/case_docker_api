machine:
    services:
        - docker

dependencies:
     override:
         - docker info

deployment:

    development:
        branch: develop
        commands:

            # For Docker Hub
            - sh deploy/deploy_dev_dockerhub_heroku.sh
            
            # For Amazon AWS
            # - sh deploy/deploy_dev_aws_heroku.sh
    
    production:
        branch: master
        commands:

            # For Docker Hub
            - sh deploy/deploy_staging_dockerhub_heroku.sh
            
            # For Amazon AWS
            #  - sh deploy/deploy_staging_aws_heroku.sh