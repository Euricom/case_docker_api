machine:
    services:
        - docker

# dependencies:
#     override:
#         - docker info

deployment:

    development:
        branch: develop
        commands:
            - sh deploy/deploy_dev.sh
    
    production:
        branch: master
        commands:
            - sh deploy/deploy_staging.sh
