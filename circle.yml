machine:
    services:
        - docker

# dependencies:
#     override:
#         - docker info

deployment:

    development:
        branch: develop
        commands:
            - deploy/deploy_dev.sh
    
    production:
        branch: master
        commands:
            - deploy/deploy_staging.sh
