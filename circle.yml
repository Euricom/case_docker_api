machine:
    services:
        - docker

# dependencies:
#     override:
#         - docker info

deployment:

    development:
        branch: develop
        commands:
            - docker build -t example123 .
            - docker save -o $CIRCLE_ARTIFACTS/example.tar registry.heroku.com/example123-dev/web
            - docker login --email=_ --username=_ --password=$HEROKU_API_KEY registry.heroku.com
            - docker push registry.heroku.com/secret-anchorage-45266/web
    
    production:
        branch: master
        commands:
            - sh deploy/deploy_staging.sh
